## web-harness

The Web Harness project offers a highly configurable all-in-one React Component for instant deep web agents.

This is done by compiling LangChain's OpenWork UI with a bundler and abstracting away Electron and platform-specific code.

The compiled and abstracted UI is then offered as a Storybook-ready React Component with a set of defaults for its runtime in web browsers.
